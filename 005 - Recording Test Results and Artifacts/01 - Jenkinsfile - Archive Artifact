pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                sh "curl www.google.com -o output.txt"
		archiveArtifacts artifacts: 'output.txt'
            }
        }
    }    
}
